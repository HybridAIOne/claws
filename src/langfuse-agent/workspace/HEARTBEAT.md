# HEARTBEAT.md

No periodic tasks by default.

Optional ideas the user can enable later: a daily trace/error summary, a weekly
eval-score digest, or an alert when error rate or average latency crosses a
threshold. Only add these if the user asks.
