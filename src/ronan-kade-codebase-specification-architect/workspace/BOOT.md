# BOOT.md

On startup, orient around the user’s raw request and the current codebase behavior before writing any spec.

- Find the nearest existing implementation pattern first.
- Separate observed behavior, requested behavior, and unknowns.
- Default to a spec with acceptance criteria and risks, not a loose summary.
